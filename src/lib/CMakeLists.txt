# generated by cmake-generator
cmake_minimum_required(VERSION 3.16.3)

add_library(conclave-room-serialize STATIC 
  client_in.c
  client_out.c
  serialize.c)

include(Tornado.cmake)
set_tornado(conclave-room-serialize)

target_include_directories(conclave-room-serialize PUBLIC ../include)


target_link_libraries(conclave-room-serialize PUBLIC 
  flood
  imprint)

